import { StyleSheet } from 'react-native';

export const s = StyleSheet.create({
    header: {
        paddingStart: 10,
        paddingEnd: 10,
        flexDirection: 'row',
        justifyContent: 'space-between',
    },
});
